# Кубанская джинния — спецификация модели

Версия 2.0 · GeckoLib · NeoForge 1.21.1

Все размеры в пикселях Minecraft: **1 блок = 16 px**.
Оси: X вправо, Y вверх, Z вперёд (лицо смотрит в −Z, как во всех моделях
Minecraft). Начало координат — уровень парения (Y=0), хвост уходит в минус.

> **Модель генерируется, а не правится руками.** Источник правды —
> `tools/texgen/genie_parts.py`; геометрия, текстура, glowmask и анимации
> собираются командой `python3 tools/texgen/gen_genie_all.py`, проверка —
> `python3 tools/texgen/check_genie.py`, силуэт в ASCII —
> `python3 tools/texgen/preview_genie.py`. Точные UV — в `UV_MAP.md`
> (тоже генерируется). Правка `.geo.json` вручную будет затёрта.

---

## 1. Габариты

| Параметр | Значение |
|---|---|
| Высота модели (макушка → кончик хвоста) | 51.7 px (3.23 блока) |
| Верх кубоида `head` | Y = 38 |
| Верх модели (`tiara_gem_c`) | Y = 39.5 |
| Линия волос (ниже неё на лице нет геометрии) | Y = 35.5 |
| Линия плеч / низ головы | Y = 30 |
| Самая широкая точка груди | Y = 26, ширина 11 px |
| Самая узкая точка талии | Y = 20, ширина 6 px |
| Пояс | Y = 15 |
| Самая широкая точка таза | Y = 13, ширина 11 px |
| Начало хвоста | Y = 11, ширина 8 px (уже таза) |
| Кончик хвоста (в покое) | Y = −12.2 |
| Размах по X (с руками) | ±12.1 px |
| Hitbox | 0.8 × 2.4 |
| Eye height | 2.05 |
| `noGravity` | true |
| Навигация | FlyingPathNavigation |

Соотношение талия/грудь = **0.55**. Ключевые направляющие (макушка, глаза,
плечи, грудь, талия, пояс, начало и кончик хвоста) заданы константами
`Y_*` и `W_*` в `genie_parts.py`, поэтому совпадают во всех видах.

Размах модели шире hitbox в 1.89 раза. Это норма: у ванильного игрока
16 px размаха при hitbox 9.6 px, то есть 1.67.

---

## 2. Иерархия костей

```
root
└── body                      ← общая волна парения
    ├── torso
    │   ├── head              ← вращается за игроком (ванильное поведение)
    │   │   ├── earring_l
    │   │   └── earring_r
    │   ├── hair_mass         ← ВАЖНО: родитель torso, НЕ head
    │   │   └── hair_tips
    │   ├── arm_l
    │   └── arm_r
    └── hips
        ├── belt
        │   ├── pendant_l
        │   ├── pendant_c
        │   ├── pendant_r
        │   └── rushnyk
        │       └── rushnyk_mid
        │           └── rushnyk_tip
        └── tail1
            └── tail2
                └── tail3
                    └── tail4
                        └── tail5
                            └── tail6
                                └── tail7
```

Кубоиды внутри костей:

| Кость | Кубоиды |
|---|---|
| `torso` | `chest`, `ribs`, `waist`, `bodice`, `necklace`, `navel_gem` |
| `head` | `head`, `hair_top`, `hair_side_l/r`, `hair_nape`, `hair_fringe`, `glasses`, `tiara`, `tiara_gem_c/l/r` |
| `hair_mass` | `hair_mass`, `hair_lock_l/r` |
| `arm_l` | `arm`, `sleeve`, `bangles` |
| `arm_r` | `arm`, `sleeve`, `bangles`, `sunflower` |
| `hips` | `hips`, `hips_low` |
| `belt` | `belt`, `belt_gem_c/l/r` |
| `tail1–7` | по одному одноимённому кубоиду |

**Что является костью, а что просто кубоидом.** Собственная кость нужна только
тому, что двигается отдельно от родителя: `head`, `hair_mass`, `hair_tips`,
`earring_l/r`, `arm_l/r`, `hips`, `belt`, `pendant_l/c/r`, `rushnyk`,
`rushnyk_mid`, `rushnyk_tip`, `tail1–7`. Остальное — обычные кубоиды внутри
родительской кости; отдельные пивоты им не нужны.

**Три решения, которые нельзя откатывать по невнимательности:**

1. `hair_mass` висит на `torso`, а не на `head`. Иначе при повороте головы за
   игроком задняя масса волос (глубиной 6 px) прошьёт спину насквозь. Стык
   маскируется золотой лентой на затылке (`hair_nape`).
2. **Перед лицом не стоит ни один кубоид волос.** Волосы на голове разнесены
   на `hair_top` (выше линии волос Y=35.5), `hair_side_l/r` (снаружи головы по
   X) и `hair_nape` (затылок). Прежняя схема с шапкой 9×9×9.5 закрывала лицо
   геометрически и держалась только на прозрачных пикселях текстуры — любая
   правка текстуры или включение mipmap закрывало глаза волосами.
3. Руки отодвинуты от торса на `ARM_GAP` **с учётом `inflate` рукавов**: зазор
   считается от раздутой границы, иначе рукав и грудь дают z-fighting на каждом
   кадре покачивания.

---

## 3. Кубоиды

> Полные таблицы координат, размеров и UV каждого кубоида —
> в **`UV_MAP.md`** (генерируется из модели). Здесь только правила,
> которые нельзя вывести из чисел.

### Силуэт «песочные часы»

Торс собран из трёх ярусов, а не из одного кубоида: `chest` (11 px),
`ribs` (8 px), `waist` (6 px). Вогнутая дуга по бокам читается именно на
стыках ярусов — единый кубоид со сглаживанием текстурой даёт «доску».
Ниже талия расширяется к тазу `hips` (11 px).

Ширины заданы константами `W_BUST`, `W_WAIST`, `W_HIP`, `W_TAIL_TOP`
в `genie_parts.py`. Менять силуэт нужно там, а не масштабом костей:
анимации крутят кости, и `scale` на них поехал бы вместе с волной парения.

### Бюджет глубин по Z

Порядок сзади вперёд закреплён константами и обязателен: задняя масса волос
(`Z_HAIR` = 2.8) проходит **за** тазом (`D_HIPS` = 5.0) и поясом
(`D_BELT` = 5.4), а подвески (`Z_PENDANT`) и рушник (`Z_RUSHNYK`) висят
**перед** ними. Без этого чёрные волосы торчат сквозь сине-фиолетовый хвост,
и это видно в каждом кадре покачивания.

### Кисти и руки

Кисть — **сплошной кубоид**, без прорези между пальцами: при 4×4 px прорезь
равна одному пикселю и читается как дырка. Рукава и браслеты имеют
`inflate 0.5`, поэтому зазор до торса считается от раздутой границы.

### Подвески и рушник

Подвески и рушник — на собственных костях с затухающим качанием. Со спины
подвески **не моделируются**, только в текстуре. Рушник — три звена
(`rushnyk` → `rushnyk_mid` → `rushnyk_tip`), суммарно 23 px: одно звено
болталось бы как доска.

### Хвост

Семь сегментов, каждый вложен в предыдущий и **заходит в родительский на
1 px по высоте** — иначе при повороте в анимации между ними откроются щели.
Ширина и глубина монотонно убывают: раздутый сегмент в середине читается как
опухоль, а не как дым. Основание (8 px) **уже таза** (11 px), поэтому пояс
визуально накрывает стык.

Все сегменты центрированы по X и Z: изгиб задаётся исключительно поворотами.

**Поза покоя — S-образная кривая в двух плоскостях.** Углы в `.geo.json`
хранятся как **разность с родителем**, потому что повороты вложенных костей
складываются. Проектировать их нужно в абсолютных углах, иначе хвост уезжает
в сторону на накопленную сумму (именно так он и уезжал на 34° в v1).

| Кость | Абс. Z (вид спереди) | Абс. X (вид сбоку) | Δ Z в geo | Δ X в geo |
|---|---|---|---|---|
| tail1 | +10° | +6° | +10° | +6° |
| tail2 | +20° | +14° | +10° | +8° |
| tail3 | +14° | +10° | −6° | −4° |
| tail4 | 0° | −2° | −14° | −12° |
| tail5 | −16° | −14° | −16° | −12° |
| tail6 | −34° | −24° | −18° | −10° |
| tail7 | −58° | −34° | −24° | −10° |

Переход абсолютного угла через ноль у `tail4` даёт перегиб, нарастание к
концу — крючок. Второй набор (по X) обязателен: без него хвост S-образный
только с одной точки обзора, а сбоку выглядит прямой палкой.

Цвета сегментов сверху вниз — ровно заданные заказчиком:
`#2A3FA8`, `#3550C4`, `#4A63D8`, `#6B5FD0`, `#8B5CF6`, `#A87CF0`, `#C9A8F7`.
`check_genie.py` проверяет, что именно этот цвет доминирует на лицевой грани:
струи дыма и дизеринг не имеют права его перебить.

## 4. Текстура

**Размер: 128×128, PNG, без сглаживания.** На 64×64 вышивка превратится в кашу, на 256×256 выбьется из стиля мода.

### Раскладка UV

Точные координаты каждого кубоида — в **`UV_MAP.md`**, он генерируется из
модели тем же кодом, что и `.geo.json`, поэтому разойтись с ней не может.
Зональной «прикидки» здесь больше нет: упаковщик атласа раскладывает
развёртки автоматически (shelf-упаковка, крупное вперёд), и любая
зафиксированная в документе картина зон устареет после первой правки.

Занято атласа: около 53%. Свободного места хватает на новые детали без
перехода на 256×256.

### Палитра

Базовые тона заданы константами в `tools/texgen/genie_parts.py`:

```
кожа          #D69E6E  #E8B68A (свет)  #B0784E (тень)  #925F3C (тень 2)
волосы        #141418  #2C2C34 (пряди)  #0A0A0C (тень)
белое полотно #F2EDE4  #D6CFC2 (тень)   #B6AEA0 (тень 2)
вышивка крас. #B01F24  #7E1418
вышивка чёрн. #1A1A1A
золото        #E8B33A  #F8D67A (блик)   #8F6418 (тень)
сапфир        #1E4FD9  #6C98FF (блик)
коралл        #C63A30  #E26858 (блик)   #8C241E (тень)
пояс бордо    #8C1A20  #601216
подсолнух     #E8B820  #F5D054 (блик)   #5C4322 (сердцевина)
хвост         7 цветов из §3 + производные тень/свет
```

Фактически в атласе **58 цветов**: к базовым добавляются машинные
производные (`_dark`, `_lite`) для верхних и нижних граней — свет сверху по
ART_BIBLE §2. Жёсткий лимит в 20 цветов, заявленный в v1, недостижим при
семицветном градиенте хвоста; `check_genie.py` предупреждает при выходе за
80 цветов — это порог, после которого палитра действительно расползается.

Красный **обязательно с чёрным контуром** — чистый красный по белому на
16 px размывается фильтрацией в розовое.

### Emissive (`_glowmask`)

Отдельным слоем светятся: камни тиары, гемы пояса, подвески, серьги,
`tail6`, `tail7`. Всё остальное в маске прозрачно — иначе персонаж светится
целиком и теряет читаемость в темноте. Маска собирается автоматически из
списка `names` в `build_glowmask`, поэтому не может разойтись с текстурой.

---

## 5. Рендер хвоста

**Слой: `cutout_no_cull` с дизерингом.**

Последние два сегмента заливаются шахматкой из полностью прозрачных пикселей: tail6 — 25% прозрачных, tail7 — 50%. Визуально читается как растворение, рендерится без артефактов.

`translucent` **не использовать** — полупрозрачные сущности в Minecraft сортируются некорректно, хвост будет просвечивать сам сквозь себя при изгибе.

---

## 6. Анимации

Файл `kuban_genie.animation.json` **генерируется** (`gen_genie_anim.py`):
ключевые кадры считаются формулой, а не набиваются руками — волна по хвосту
это синус с фазовым сдвигом на сегмент, и правка амплитуды вручную
неизбежно разъезжается между семью костями. Генератор дополнительно
сверяет каждое имя кости со списком из `genie_parts.py`, поэтому
переименование кости ломает сборку, а не даёт мёртвый канал в игре.

Длительности в GeckoLib задаются в **секундах**, не в тиках.

| Имя | Длит. | Loop | Содержимое |
|---|---|---|---|
| `idle` | 3.0 с | да | `body` ±1 px по Y; волна по хвосту, сдвиг фазы π/6 на сегмент, амплитуда 4°; волосы, рушник и серьги качаются с затуханием |
| `move` | 2.0 с | да | то же, амплитуда ×2, наклон `body` вперёд на 5° |
| `greet` | 1.5 с | нет | правая рука к груди, поклон головы 12° — казачий жест |
| `wish` | 2.25 с | нет | обе руки вперёд-вверх, корпус приподнимается, хвост подтягивается |
| `cast` | 2.5 с | нет | левая рука вверх, хвост закручивается в спираль, рушник разворачивается всеми тремя звеньями |
| `spawn` | 1.75 с | нет | хвост вытягивается снизу вверх: сегменты появляются с масштабом 0→1 с задержкой 0.1 с |
| `despawn` | 1.5 с | нет | обратный порядок — первым исчезает кончик |
| `hurt` | 0.5 с | нет | короткая тряска `body` |

Формула волны:

```
angle(i, t) = -A · sin(2π·t/T − i·φ)
φ = π/6 на сегмент
A = 4° (idle) / 8° (move)
```

Затухающие подвесы используют ту же волну с множителем амплитуды и
задержкой: `hair_mass` 0.75/0 с, `hair_tips` 0.45/0.2 с, `rushnyk` 0.62/0 с,
`rushnyk_mid` 0.40/0.2 с, `rushnyk_tip` 0.26/0.4 с. Кончик всегда идёт за
основанием, поэтому масса читается как ткань и волосы, а не как жёсткая деталь.

---

## 7. Решённые вопросы дизайна

Все пункты закрыты в v2; ниже — принятые решения и причины, чтобы их не
откатили обратно.

- **Асимметрия подсолнуха** — оставлена. Подсолнух только на одном плече,
  это осознанная черта персонажа. Руки не зеркалятся, каждая занимает свою
  область атласа; места хватает (занято ~53%).
- **Основание хвоста** — сужено до 8 px против 11 px таза, пояс визуально
  накрывает стык. `check_genie.py` падает, если основание перестанет быть
  уже таза.
- **Талия** — 6 px против 11 px груди (соотношение 0.55). Переход разбит на
  три яруса (`chest` → `ribs` → `waist`), поэтому щелей при изгибе нет.
- **Рушник** — удлинён до трёх звеньев, 23 px. Свисает слева, обрамляет бок
  и верх хвоста, центр персонажа не перекрывает.
- **Задняя масса волос** — пряди двух тонов (`#2C2C34` светлее, `#0A0A0C`
  темнее) с переменным шагом, поэтому на дистанции не читается дырой.
- **Вырез лифа** — сплошная линия края, вертикального разрыва нет; по центру
  только золотая линия.
- **Живот** — открыт, с сапфиром пупка и золотой цепочкой вверх к лифу;
  по бокам талии и под грудью теневая проработка.
- **Наручи в заднем виде** — тёмная нижняя кромка (`GOLD_DK`) на браслетах,
  с рукавами не сливаются.

---

## 8. Глечик (отдельная сущность)

Одна кость `jug`, пивот `0, 0, 0`. Y=0 — дно, общая высота 12 px.
Отдельные кости не нужны: глечик вращается целиком.
Генерируется скриптом `tools/texgen/gen_jug.py`, атлас 64×64.

**Горло действительно открытое.** Это главное требование, и оно решается
геометрией, а не текстурой. В v1 «внутренность» была плоской пластиной
4×1×4 **над** венчиком: сверху она читалась как крышка. Сейчас все ярусы —
тулово выше дна шахты, плечи, горло и венчик — собраны **кольцами** из
четырёх стенок, поэтому по центру проходит настоящее отверстие радиусом
1.5 px до дна на Y=5, то есть **6 px видимой глубины**. Сплошной кубоид на
любом из этих ярусов немедленно возвращает эффект «закрытой крышки»;
`verify()` в `gen_jug.py` пускает луч сверху вниз и падает, если он
упирается во что-либо выше дна.

| Ярус | Кубоиды | Y | Прим. |
|---|---|---|---|
| поддон | `base` | 0–4.6 | сплошной, ниже дна шахты |
| тулово | `body_n/s/e/w` | 4.6–8 | кольцо, красно-чёрная полоса |
| плечи | `shoulder_n/s/e/w` | 2–7 | кольцо, широкая часть |
| горло | `neck_n/s/e/w` | 8–11 | кольцо |
| венчик | `rim_n/s/e/w` | 11–12 | кольцо, внутренние торцы — цвет нутра |
| дно шахты | `bore_floor` | 4.6–5 | `#1C100A`, тёмное нутро |
| ручка | `handle` | 4–9 | одна, сбоку |

Красно-чёрная геометрическая полоса на тулове — те же мотивы, что на
костюме. Медленное вращение вокруг Y. При `spawn` джиннии — партиклы из
горла.

---

## 9. Стек и сборка

- **GeckoLib** для NeoForge 1.21.1, `AutoGlowingGeoLayer` для emissive-слоя.
- Рендер: `RenderTypes.entityCutout` — см. §5 о недопустимости `translucent`.
- Blockbench нужен только для просмотра: ассеты собираются скриптами.

```
python3 tools/texgen/gen_genie_all.py   # геометрия → текстура → анимации → проверка
python3 tools/texgen/gen_jug.py         # глечик
python3 tools/texgen/gen_uv_map.py      # UV_MAP.md
python3 tools/texgen/preview_genie.py   # силуэт в ASCII: front/side/top
python3 tools/texgen/check_genie.py     # только проверка, код возврата для CI
```

Порядок в `gen_genie_all.py` обязателен: упаковщик атласа перекладывает UV
при любом изменении набора кубоидов, поэтому текстуру нельзя рисовать
раньше геометрии — узор уедет на чужие грани.

Что проверяет `check_genie.py`: пересечения кубоидов разных костей (кроме
разрешённых пар «одежда на теле»), выход UV за атлас, наложение развёрток,
силуэт «песочные часы», монотонность и S-образность хвоста в двух
плоскостях, точное совпадение семи цветов хвоста, закраску каждой грани,
родителя `hair_mass`, совпадение `.geo.json` с генератором и существование
всех костей, которые крутят анимации.
